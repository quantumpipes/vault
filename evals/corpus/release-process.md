# Release Process

A production release requires two approvals and a green CI pipeline. During a code freeze before a major release, no new features may land: only critical bug fixes are permitted. Release notes are mandatory.
